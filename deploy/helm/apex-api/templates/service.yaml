apiVersion: v1
kind: Service
metadata:
  name: { { include "apex-api.fullname" . } }
  labels: { { - include "apex-api.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "apex-api.selectorLabels" . | nindent 4 } }
