apiVersion: v1
kind: Service
metadata:
  name: {{ include "apex-web.fullname" . }}
  labels: {{- include "apex-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "apex-web.selectorLabels" . | nindent 4 }}
