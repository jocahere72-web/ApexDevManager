
  CREATE OR REPLACE EDITIONABLE FUNCTION "STANDARD_HASH_OUTPUT" (str IN VARCHAR2, method in varchar2 default 'SHA1')
  RETURN VARCHAR2
  AS
    op VARCHAR2(100);
  BEGIN
  SELECT STANDARD_HASH(str, method) INTO op FROM dual;
  RETURN op;
END;
/

