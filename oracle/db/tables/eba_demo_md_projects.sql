
  CREATE TABLE "EBA_DEMO_MD_PROJECTS"
   (	"ID" NUMBER NOT NULL ENABLE,
	"PROJECT_LEAD" NUMBER,
	"NAME" VARCHAR2(255) NOT NULL ENABLE,
	"DESCRIPTION" VARCHAR2(4000),
	"STATUS_CD" VARCHAR2(15) NOT NULL ENABLE,
	"COMPLETED_DATE" DATE,
	"CREATED" TIMESTAMP (6) WITH LOCAL TIME ZONE NOT NULL ENABLE,
	"CREATED_BY" VARCHAR2(255) NOT NULL ENABLE,
	"UPDATED" TIMESTAMP (6) WITH LOCAL TIME ZONE NOT NULL ENABLE,
	"UPDATED_BY" VARCHAR2(255) NOT NULL ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_PROJECTS_PK" PRIMARY KEY ("ID")
  USING INDEX  ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_PROJECTS_UK" UNIQUE ("NAME")
  USING INDEX  ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_TEAM_MEMBER_FK" FOREIGN KEY ("PROJECT_LEAD")
	  REFERENCES "EBA_DEMO_MD_TEAM_MEMBERS" ("ID") ON DELETE SET NULL ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_STATUS_FK" FOREIGN KEY ("STATUS_CD")
	  REFERENCES "EBA_DEMO_MD_STATUS" ("CD") ON DELETE SET NULL ENABLE
   ) ;

