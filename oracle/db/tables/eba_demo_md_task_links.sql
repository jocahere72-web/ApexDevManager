
  CREATE TABLE "EBA_DEMO_MD_TASK_LINKS"
   (	"ID" NUMBER NOT NULL ENABLE,
	"PROJECT_ID" NUMBER NOT NULL ENABLE,
	"TASK_ID" NUMBER NOT NULL ENABLE,
	"LINK_TYPE" VARCHAR2(20) NOT NULL ENABLE,
	"URL" VARCHAR2(255),
	"APPLICATION_ID" NUMBER,
	"APPLICATION_PAGE" NUMBER,
	"DESCRIPTION" VARCHAR2(4000),
	"CREATED" TIMESTAMP (6) WITH LOCAL TIME ZONE NOT NULL ENABLE,
	"CREATED_BY" VARCHAR2(255) NOT NULL ENABLE,
	"UPDATED" TIMESTAMP (6) WITH LOCAL TIME ZONE NOT NULL ENABLE,
	"UPDATED_BY" VARCHAR2(255) NOT NULL ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_TASK_LINKS_PK" PRIMARY KEY ("ID")
  USING INDEX  ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_TASK_LINK_LTY_CH" CHECK ( link_type in ('URL','Application')) ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_TASK_LINK_PRJ_FK" FOREIGN KEY ("PROJECT_ID")
	  REFERENCES "EBA_DEMO_MD_PROJECTS" ("ID") ON DELETE CASCADE ENABLE,
	 CONSTRAINT "EBA_DEMO_MD_TASK_LINK_TSK_FK" FOREIGN KEY ("TASK_ID")
	  REFERENCES "EBA_DEMO_MD_TASKS" ("ID") ON DELETE CASCADE ENABLE
   ) ;

