
  CREATE TABLE "EBA_DEMO_CHART_SAMPLE_DATA"
   (	"ID" NUMBER NOT NULL ENABLE,
	"SAMPLE_ID" NUMBER,
	"RESPONSE" NUMBER,
	"CREATED" TIMESTAMP (6) WITH LOCAL TIME ZONE,
	"CREATED_BY" VARCHAR2(255),
	"UPDATED" TIMESTAMP (6) WITH LOCAL TIME ZONE,
	"UPDATED_BY" VARCHAR2(255),
	 CONSTRAINT "EBA_DEMO_SAMPLE_DATA_PK" PRIMARY KEY ("ID")
  USING INDEX  ENABLE,
	 CONSTRAINT "EBA_DEMO_SAMPLE_FK" FOREIGN KEY ("SAMPLE_ID")
	  REFERENCES "EBA_DEMO_CHART_SAMPLE_NAMES" ("ID") ON DELETE CASCADE ENABLE
   ) ;

